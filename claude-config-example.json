{
  "mcpServers": {
    "mongodb": {
      "command": "npx",
      "args": ["@yourusername/mongodb-that-works"],
      "env": {
        "MONGODB_URI": "mongodb+srv://username:password@cluster.mongodb.net/database",
        "MONGODB_DATABASE": "your_database_name"
      }
    }
  }
}